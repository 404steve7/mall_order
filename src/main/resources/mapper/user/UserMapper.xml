<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.henry.mallorder.user.mapper.UserMapper">

    <insert id="insert" parameterType="com.henry.mallorder.user.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_info(
            username,
            password,
            nickname,
            status
        ) VALUES (
            #{username},
            #{password},
            #{nickname},
            #{status}
        )
    </insert>

    <select id="selectByUsername" resultType="com.henry.mallorder.user.entity.User">
        SELECT
            id,
            username,
            password,
            nickname,
            status,
            create_time,
            update_time
        FROM user_info
        WHERE username = #{username}
    </select>

    <select id="selectById" resultType="com.henry.mallorder.user.entity.User">
        SELECT
            id,
            username,
            password,
            nickname,
            status,
            create_time,
            update_time
        FROM user_info
        WHERE id = #{id}
    </select>
</mapper>