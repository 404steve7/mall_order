spring:
  application:
    name: mall-order-demo
  autoconfigure:
    exclude:
      # Remove this once spring.datasource.* is configured.
      - org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
